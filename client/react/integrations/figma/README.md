Where to create a Figma token (link)

How the token is stored: OS keychain via Tauri

SPA behavior:

Desktop (Tauri): full MCP support

Web build: MCP disabled by default (no secure token), user can point to their own proxy if they insist
