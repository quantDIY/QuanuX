# SignalR Bridge (Node) — Scaffold

Alternate runtime using `@microsoft/signalr`. No live wiring yet.

Run:
```bash
pnpm install --prefix server/bridges/signalr/node
node server/bridges/signalr/node/index.mjs
