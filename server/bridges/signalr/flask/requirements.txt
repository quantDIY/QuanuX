# SignalR Flask Bridge (scaffold)
Flask>=3.0
flask-sock
gunicorn
Werkzeug>=3.0
itsdangerous>=2.1

# Optional future Python SignalR client (enable when wiring real hubs)
signalrcore>=0.9.0
requests>=2.31.0
websocket-client
