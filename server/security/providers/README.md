# Provider Auth Hooks (Scaffold)

Place per-provider auth helpers here (e.g., OAuth2 exchange handlers).
Each provider folder should include:
- README.md (link to official docs)
- manifest.yaml (scopes, base URLs)
- auth.py (exchange/refresh helpers; no secrets)
