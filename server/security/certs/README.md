# mTLS Placeholders (Do Not Commit Real Keys)

This folder exists only to document where local dev certificates would live.
In CI/production, certificates are provisioned by the platform (e.g., Kubernetes
secrets, Vault injectors). Never commit private keys.
