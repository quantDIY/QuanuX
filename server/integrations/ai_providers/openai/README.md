# OpenAI (Scaffold)
- Base URL: https://api.openai.com/v1
- Auth: API Key (OPENAI_API_KEY via Secrets Broker)
- Capabilities: chat, embeddings, tools, streaming

Setup:
1) Obtain an API key from OpenAI
2) Store it via the QuanuX Secrets Broker (not in git)
3) Select model explicitly per call (v0.0.1)
