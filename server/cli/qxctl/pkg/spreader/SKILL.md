---
name: Spreader Module Bounds
description: Guidelines for operating the qxctl spreader module logic natively.
---

# `qxctl spreader` Architecture & Operations

## Purpose
The `spreader` module maps cython arrays, transpile logic grids, and binds execution boundaries into the QuanuX loop explicitly cleanly.

## Relevant Commands
1. **`spreader deploy`**
   - **Capability/Risk:** `CapDeploy` | `RiskDangerous`
   - **Workflow:** Deploys the locally compiled node cleanly into production hardware limits natively. 
   - **Structured Output:** Strictly outputs JSON (`--output=json`) and intercepts bounds with 403 blocks uniformly correctly.
2. **`spreader package`**
   - **Capability/Risk:** `CapSimulate` | `RiskStable`
   - **Workflow:** Wraps Cython execution strategies efficiently accurately structurally securely. 
   - **Structured Output:** Operates dynamically safely correctly outputting JSON boundaries successfully. 

## Caveats and Exceptions
The core mutation loop inside `spreader package` acts safely utilizing mocked `exec.CommandContext` parameters natively to protect the underlying host securely accurately cleanly.

## References
Consult `docs/md/qxctl_spreader.md` natively for physical parameter structures safely securely.
