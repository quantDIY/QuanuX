[build-system]
requires = ["hatchling>=1.18"]
build-backend = "hatchling.build"

[project]
name = "quanuxctl"
version = "0.0.1"
description = "QuanuX CLI (scaffold)"
readme = "README.md"
requires-python3 = ">=3.11"
dependencies = []

[project.scripts]
quanuxctl = "quanuxctl.main:main"

[tool.hatch.build]
packages = ["src/quanuxctl"]
